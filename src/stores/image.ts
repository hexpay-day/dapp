// export const renderHexIcon = (className = '') => (
//   `<span><img width="20" height="20" class="${className}" alt="three pink hexagons nested inside of one another" src="/hex.svg" /></span>`
// )

export const renderHedronIcon = (title: string, className = '') => (
  `<span class="w-6 inline-block"><img width="20" height="20" class="${className}" alt="a white dodecahedron on a blue background" src="/hedron.png" title="${title}" /></span>`
)
